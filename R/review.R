

review <- function(data, paramters = NULL, outfile=NULL){
  
  browser()
  
  if (!is.null(paramters)){
    
    
  }
  
  
  rows <- sum(as.)
  imputed <- sum(as.numeric(data$imputed))
  
  
  
  
}